services:
  coach-ia:
    build: .
    ports:
      - "3001:3000"  # opcional al usar nginx-proxy-manager
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
      - ./prisma/dev.db:/app/prisma/dev.db
    environment:
      - NODE_ENV=development
      - NEXTAUTH_URL=https://gym.homelab989.duckdns.org
      - NEXTAUTH_SECRET=coach-ia-super-secret-jwt-key-2024
      - GEMINI_API_KEY=AIzaSyBbkqF0czucTAwUwXSL1i65gczdq11MSw0
    command: npm run dev
    networks:
      - inmich_net

networks:
  inmich_net:
    external: true